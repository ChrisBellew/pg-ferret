apiVersion: 1

datasources:
  - name: Tempo
    uid: tempo
    type: tempo
    access: proxy
    url: http://localhost:3100
    isDefault: true
    jsonData:
      httpMethod: POST
    editable: true
